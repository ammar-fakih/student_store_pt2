const db = require('../db');

class Order {
  static listOrdersForUser(userId) {
    // return all orders that the authenticated user has created
  }

  static createOrder(order) {
    // create an order for the authenticated user
  }
}
